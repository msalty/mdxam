{
  "name": "mdxam",
  "version": "2.4.1",
  "private": true,
  "type": "module",
  "description": "A dependency-free, offline-first Markdown practice exam app.",
  "scripts": {
    "test": "node --test",
    "check": "node --check main.js && node --check sw.js"
  },
  "engines": {
    "node": ">=20"
  }
}
